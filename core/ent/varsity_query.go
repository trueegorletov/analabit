// Code generated by ent, DO NOT EDIT.

package ent

import (
	"github.com/trueegorletov/analabit/core/ent/heading"
	"github.com/trueegorletov/analabit/core/ent/predicate"
	"github.com/trueegorletov/analabit/core/ent/varsity"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// VarsityQuery is the builder for querying Varsity entities.
type VarsityQuery struct {
	config
	ctx          *QueryContext
	order        []varsity.OrderOption
	inters       []Interceptor
	predicates   []predicate.Varsity
	withHeadings *HeadingQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VarsityQuery builder.
func (vq *VarsityQuery) Where(ps ...predicate.Varsity) *VarsityQuery {
	vq.predicates = append(vq.predicates, ps...)
	return vq
}

// Limit the number of records to be returned by this query.
func (vq *VarsityQuery) Limit(limit int) *VarsityQuery {
	vq.ctx.Limit = &limit
	return vq
}

// Offset to start from.
func (vq *VarsityQuery) Offset(offset int) *VarsityQuery {
	vq.ctx.Offset = &offset
	return vq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vq *VarsityQuery) Unique(unique bool) *VarsityQuery {
	vq.ctx.Unique = &unique
	return vq
}

// Order specifies how the records should be ordered.
func (vq *VarsityQuery) Order(o ...varsity.OrderOption) *VarsityQuery {
	vq.order = append(vq.order, o...)
	return vq
}

// QueryHeadings chains the current query on the "headings" edge.
func (vq *VarsityQuery) QueryHeadings() *HeadingQuery {
	query := (&HeadingClient{config: vq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(varsity.Table, varsity.FieldID, selector),
			sqlgraph.To(heading.Table, heading.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, varsity.HeadingsTable, varsity.HeadingsColumn),
		)
		fromU = sqlgraph.SetNeighbors(vq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Varsity entity from the query.
// Returns a *NotFoundError when no Varsity was found.
func (vq *VarsityQuery) First(ctx context.Context) (*Varsity, error) {
	nodes, err := vq.Limit(1).All(setContextOp(ctx, vq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{varsity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vq *VarsityQuery) FirstX(ctx context.Context) *Varsity {
	node, err := vq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Varsity ID from the query.
// Returns a *NotFoundError when no Varsity ID was found.
func (vq *VarsityQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vq.Limit(1).IDs(setContextOp(ctx, vq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{varsity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vq *VarsityQuery) FirstIDX(ctx context.Context) int {
	id, err := vq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Varsity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Varsity entity is found.
// Returns a *NotFoundError when no Varsity entities are found.
func (vq *VarsityQuery) Only(ctx context.Context) (*Varsity, error) {
	nodes, err := vq.Limit(2).All(setContextOp(ctx, vq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{varsity.Label}
	default:
		return nil, &NotSingularError{varsity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vq *VarsityQuery) OnlyX(ctx context.Context) *Varsity {
	node, err := vq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Varsity ID in the query.
// Returns a *NotSingularError when more than one Varsity ID is found.
// Returns a *NotFoundError when no entities are found.
func (vq *VarsityQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vq.Limit(2).IDs(setContextOp(ctx, vq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{varsity.Label}
	default:
		err = &NotSingularError{varsity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vq *VarsityQuery) OnlyIDX(ctx context.Context) int {
	id, err := vq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Varsities.
func (vq *VarsityQuery) All(ctx context.Context) ([]*Varsity, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryAll)
	if err := vq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Varsity, *VarsityQuery]()
	return withInterceptors[[]*Varsity](ctx, vq, qr, vq.inters)
}

// AllX is like All, but panics if an error occurs.
func (vq *VarsityQuery) AllX(ctx context.Context) []*Varsity {
	nodes, err := vq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Varsity IDs.
func (vq *VarsityQuery) IDs(ctx context.Context) (ids []int, err error) {
	if vq.ctx.Unique == nil && vq.path != nil {
		vq.Unique(true)
	}
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryIDs)
	if err = vq.Select(varsity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vq *VarsityQuery) IDsX(ctx context.Context) []int {
	ids, err := vq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vq *VarsityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryCount)
	if err := vq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, vq, querierCount[*VarsityQuery](), vq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (vq *VarsityQuery) CountX(ctx context.Context) int {
	count, err := vq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vq *VarsityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, vq.ctx, ent.OpQueryExist)
	switch _, err := vq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (vq *VarsityQuery) ExistX(ctx context.Context) bool {
	exist, err := vq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VarsityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vq *VarsityQuery) Clone() *VarsityQuery {
	if vq == nil {
		return nil
	}
	return &VarsityQuery{
		config:       vq.config,
		ctx:          vq.ctx.Clone(),
		order:        append([]varsity.OrderOption{}, vq.order...),
		inters:       append([]Interceptor{}, vq.inters...),
		predicates:   append([]predicate.Varsity{}, vq.predicates...),
		withHeadings: vq.withHeadings.Clone(),
		// clone intermediate query.
		sql:  vq.sql.Clone(),
		path: vq.path,
	}
}

// WithHeadings tells the query-builder to eager-load the nodes that are connected to
// the "headings" edge. The optional arguments are used to configure the query builder of the edge.
func (vq *VarsityQuery) WithHeadings(opts ...func(*HeadingQuery)) *VarsityQuery {
	query := (&HeadingClient{config: vq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	vq.withHeadings = query
	return vq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Varsity.Query().
//		GroupBy(varsity.FieldCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (vq *VarsityQuery) GroupBy(field string, fields ...string) *VarsityGroupBy {
	vq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &VarsityGroupBy{build: vq}
	grbuild.flds = &vq.ctx.Fields
	grbuild.label = varsity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//	}
//
//	client.Varsity.Query().
//		Select(varsity.FieldCode).
//		Scan(ctx, &v)
func (vq *VarsityQuery) Select(fields ...string) *VarsitySelect {
	vq.ctx.Fields = append(vq.ctx.Fields, fields...)
	sbuild := &VarsitySelect{VarsityQuery: vq}
	sbuild.label = varsity.Label
	sbuild.flds, sbuild.scan = &vq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a VarsitySelect configured with the given aggregations.
func (vq *VarsityQuery) Aggregate(fns ...AggregateFunc) *VarsitySelect {
	return vq.Select().Aggregate(fns...)
}

func (vq *VarsityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range vq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, vq); err != nil {
				return err
			}
		}
	}
	for _, f := range vq.ctx.Fields {
		if !varsity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vq.path != nil {
		prev, err := vq.path(ctx)
		if err != nil {
			return err
		}
		vq.sql = prev
	}
	return nil
}

func (vq *VarsityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Varsity, error) {
	var (
		nodes       = []*Varsity{}
		_spec       = vq.querySpec()
		loadedTypes = [1]bool{
			vq.withHeadings != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Varsity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Varsity{config: vq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := vq.withHeadings; query != nil {
		if err := vq.loadHeadings(ctx, query, nodes,
			func(n *Varsity) { n.Edges.Headings = []*Heading{} },
			func(n *Varsity, e *Heading) { n.Edges.Headings = append(n.Edges.Headings, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (vq *VarsityQuery) loadHeadings(ctx context.Context, query *HeadingQuery, nodes []*Varsity, init func(*Varsity), assign func(*Varsity, *Heading)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Varsity)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Heading(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(varsity.HeadingsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.varsity_headings
		if fk == nil {
			return fmt.Errorf(`foreign-key "varsity_headings" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "varsity_headings" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (vq *VarsityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vq.querySpec()
	_spec.Node.Columns = vq.ctx.Fields
	if len(vq.ctx.Fields) > 0 {
		_spec.Unique = vq.ctx.Unique != nil && *vq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, vq.driver, _spec)
}

func (vq *VarsityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(varsity.Table, varsity.Columns, sqlgraph.NewFieldSpec(varsity.FieldID, field.TypeInt))
	_spec.From = vq.sql
	if unique := vq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if vq.path != nil {
		_spec.Unique = true
	}
	if fields := vq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, varsity.FieldID)
		for i := range fields {
			if fields[i] != varsity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vq *VarsityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vq.driver.Dialect())
	t1 := builder.Table(varsity.Table)
	columns := vq.ctx.Fields
	if len(columns) == 0 {
		columns = varsity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vq.sql != nil {
		selector = vq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vq.ctx.Unique != nil && *vq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range vq.predicates {
		p(selector)
	}
	for _, p := range vq.order {
		p(selector)
	}
	if offset := vq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// VarsityGroupBy is the group-by builder for Varsity entities.
type VarsityGroupBy struct {
	selector
	build *VarsityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vgb *VarsityGroupBy) Aggregate(fns ...AggregateFunc) *VarsityGroupBy {
	vgb.fns = append(vgb.fns, fns...)
	return vgb
}

// Scan applies the selector query and scans the result into the given value.
func (vgb *VarsityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vgb.build.ctx, ent.OpQueryGroupBy)
	if err := vgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VarsityQuery, *VarsityGroupBy](ctx, vgb.build, vgb, vgb.build.inters, v)
}

func (vgb *VarsityGroupBy) sqlScan(ctx context.Context, root *VarsityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(vgb.fns))
	for _, fn := range vgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*vgb.flds)+len(vgb.fns))
		for _, f := range *vgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*vgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// VarsitySelect is the builder for selecting fields of Varsity entities.
type VarsitySelect struct {
	*VarsityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (vs *VarsitySelect) Aggregate(fns ...AggregateFunc) *VarsitySelect {
	vs.fns = append(vs.fns, fns...)
	return vs
}

// Scan applies the selector query and scans the result into the given value.
func (vs *VarsitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vs.ctx, ent.OpQuerySelect)
	if err := vs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VarsityQuery, *VarsitySelect](ctx, vs.VarsityQuery, vs, vs.inters, v)
}

func (vs *VarsitySelect) sqlScan(ctx context.Context, root *VarsityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(vs.fns))
	for _, fn := range vs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*vs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
